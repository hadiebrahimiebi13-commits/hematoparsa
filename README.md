# 📚 خلاصه جامع هماتولوژی

یک مجموعه کامل و حرفه‌ای از خلاصه فصول هماتولوژی برای دانشجویان پزشکی

## 🗂️ ساختار پروژه

برای استفاده صحیح از این پروژه، ساختار فایل‌ها باید به شکل زیر باشد:

```
hematology-summary/
│
├── index.html                 # 👈 فایل اصلی (صفحه اول)
│
├── chapters/                  # 👈 پوشه فصول
│   ├── chapter01.html        # فصل ۱
│   ├── chapter02.html        # فصل ۲
│   ├── chapter03.html        # فصل ۳
│   ├── chapter04.html        # فصل ۴
│   ├── chapter05.html        # فصل ۵
│   ├── chapter06.html        # فصل ۶
│   ├── chapter07.html        # فصل ۷
│   ├── chapter08.html        # فصل ۸
│   ├── chapter09.html        # فصل ۹
│   └── chapter10.html        # فصل ۱۰ (نمونه فایلی که ساخته شد)
│
└── README.md                  # این فایل
```

## 📝 نحوه استفاده

### مرحله ۱: ایجاد ساختار پوشه‌ها

1. یک پوشه جدید با نام دلخواه (مثلاً `hematology-summary`) بسازید
2. فایل `index.html` را در ریشه این پوشه قرار دهید
3. یک پوشه با نام **`chapters`** در کنار فایل `index.html` بسازید

### مرحله ۲: قرار دادن فایل‌های فصول

فایل‌های HTML هر فصل را در پوشه `chapters` قرار دهید با نام‌گذاری زیر:

- `chapter01.html` → فصل ۱
- `chapter02.html` → فصل ۲
- `chapter03.html` → فصل ۳
- و همینطور تا `chapter10.html`

⚠️ **مهم:** نام فایل‌ها باید دقیقاً مطابق با نام‌هایی باشد که در فایل `index.html` تعریف شده است.

### مرحله ۳: تنظیم فصول موجود

در فایل `index.html` در قسمت JavaScript (خط ۳۰۷ به بعد)، آرایه `chapters` را ویرایش کنید:

```javascript
const chapters = [
    {
        number: 1,
        title: "عنوان فصل ۱",
        file: "chapter01.html",
        available: true  // 👈 true = فایل موجود | false = فایل موجود نیست
    },
    {
        number: 2,
        title: "عنوان فصل ۲",
        file: "chapter02.html",
        available: true
    },
    // ... بقیه فصول
];
```

- اگر فایل فصلی آماده است → `available: true`
- اگر فایل فصلی هنوز آماده نیست → `available: false`

## 🚀 راه‌اندازی پروژه

### روش ۱: اجرای محلی (Local)

1. فایل `index.html` را در مرورگر باز کنید
2. ⚠️ **توجه:** به دلیل محدودیت‌های CORS، ممکن است در برخی مرورگرها کار نکند
3. راه حل: از یک وب سرور محلی استفاده کنید:

```bash
# با Python 3
python -m http.server 8000

# یا با Python 2
python -m SimpleHTTPServer 8000

# سپس مرورگر را باز کرده و به آدرس زیر بروید:
# http://localhost:8000
```

### روش ۲: استفاده از GitHub Pages (پیشنهادی ⭐)

#### مرحله ۱: آپلود پروژه به GitHub

1. یک Repository جدید در GitHub بسازید
2. فایل‌ها را آپلود کنید:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/YOUR-REPO-NAME.git
git push -u origin main
```

#### مرحله ۲: فعال‌سازی GitHub Pages

1. به Repository خود در GitHub بروید
2. روی **Settings** کلیک کنید
3. در منوی سمت چپ، **Pages** را انتخاب کنید
4. در بخش **Source**:
   - Branch را روی `main` قرار دهید
   - Folder را روی `/ (root)` قرار دهید
5. روی **Save** کلیک کنید

#### مرحله ۳: دسترسی به سایت

پس از چند دقیقه، سایت شما در آدرس زیر در دسترس خواهد بود:

```
https://YOUR-USERNAME.github.io/YOUR-REPO-NAME/
```

## 🎨 سفارشی‌سازی

### تغییر عناوین فصول

در فایل `index.html`، آرایه `chapters` را ویرایش کنید:

```javascript
{
    number: 10,
    title: "عنوان دلخواه شما",  // 👈 اینجا را تغییر دهید
    file: "chapter10.html",
    available: true
}
```

### تغییر رنگ‌ها

در بخش `<style>` فایل `index.html`، متغیرهای رنگی را تغییر دهید:

```css
/* رنگ اصلی سایدبار */
background: linear-gradient(180deg, #1e293b 0%, #0f172a 100%);

/* رنگ فصل فعال */
.chapter-item.active {
    border-right-color: #6366f1;  /* 👈 رنگ را تغییر دهید */
}
```

## 📱 پشتیبانی از موبایل

پروژه به صورت کامل **Responsive** است و در موبایل به خوبی کار می‌کند:

- در موبایل، منوی سایدبار به صورت پنهان است
- با کلیک روی دکمه همبرگر (☰) در بالای صفحه، منو باز می‌شود
- با انتخاب فصل، منو به صورت خودکار بسته می‌شود

## 🔧 عیب‌یابی

### فصل بارگذاری نمی‌شود

**علت:** فایل در مسیر صحیح قرار ندارد

**راه حل:**
1. مطمئن شوید پوشه `chapters` در کنار `index.html` قرار دارد
2. نام فایل را با دقت بررسی کنید (حساس به حروف کوچک/بزرگ)
3. از DevTools مرورگر (F12) برای مشاهده خطاها استفاده کنید

### صفحه خالی نمایش داده می‌شود

**علت:** خطای CORS در اجرای محلی

**راه حل:**
- از وب سرور محلی استفاده کنید (Python HTTP Server)
- یا پروژه را در GitHub Pages هاست کنید

### فایل فصل نمایش داده نمی‌شود

**علت:** محتوای HTML فصل ممکن است script های خارجی داشته باشد

**راه حل:**
- در فایل‌های فصول، script های `src` خارجی را بررسی کنید
- CDN های استفاده شده (مثل Tailwind CSS) باید در دسترس باشند

## 📄 نحوه ساخت فایل‌های فصل

هر فایل فصل باید یک سند HTML کامل باشد:

```html
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>فصل X</title>
    <!-- CDN ها و استایل‌ها -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- محتوای فصل -->
</body>
</html>
```

⚠️ **نکته مهم:** فایل `index.html` فقط محتوای داخل تگ `<body>` را استخراج و نمایش می‌دهد.

## 🎯 ویژگی‌ها

✅ طراحی مدرن و حرفه‌ای
✅ سایدبار ثابت با لیست فصول
✅ بارگذاری داینامیک فصول
✅ پشتیبانی کامل از موبایل (Responsive)
✅ انیمیشن‌های روان
✅ صفحه خوش‌آمدگویی جذاب
✅ نمایش وضعیت بارگذاری
✅ مدیریت خطا

## 📞 پشتیبانی

در صورت بروز مشکل:
1. کنسول مرورگر (F12 → Console) را بررسی کنید
2. از Developer Tools برای دیباگ استفاده کنید
3. مطمئن شوید ساختار پوشه‌ها صحیح است

## 📜 مجوز

این پروژه برای استفاده آموزشی دانشجویان پزشکی طراحی شده است.

---

**نکته نهایی:** پس از آپلود پروژه در GitHub Pages، لینک سایت خود را با دوستان به اشتراک بگذارید! 🎉
