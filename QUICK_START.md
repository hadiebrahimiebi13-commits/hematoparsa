# 🚀 راهنمای سریع شروع کار

## گام اول: دانلود فایل‌ها

فایل‌های زیر را دانلود کنید:
- ✅ `index.html` - فایل اصلی
- ✅ `chapter10.html` - فصل ۱۰ (نمونه آماده)
- ✅ `chapter01_template.html` - قالب برای فصول دیگر
- ✅ `README.md` - راهنمای کامل

---

## گام دوم: ساخت ساختار پوشه‌ها

### روش ۱: دستی

```
📁 پوشه اصلی پروژه (مثلاً hematology-summary)
│
├── 📄 index.html
│
└── 📁 chapters
    ├── 📄 chapter01.html
    ├── 📄 chapter02.html
    ├── 📄 chapter03.html
    ├── ...
    └── 📄 chapter10.html
```

### مراحل:
1. یک پوشه جدید بسازید (مثلاً `hematology-summary`)
2. فایل `index.html` را در آن قرار دهید
3. یک پوشه با نام **دقیقاً** `chapters` بسازید
4. فایل `chapter10.html` را در پوشه `chapters` قرار دهید

---

## گام سوم: اضافه کردن فصول جدید

### روش آسان:

1. فایل `chapter01_template.html` را کپی کنید
2. نام آن را تغییر دهید (مثلاً به `chapter02.html`)
3. محتوای داخل `<body>` را با محتوای فصل خود جایگزین کنید
4. فایل را در پوشه `chapters/` قرار دهید
5. در فایل `index.html`، وضعیت فصل را به `available: true` تغییر دهید

---

## گام چهارم: تنظیم فایل index.html

فایل `index.html` را باز کنید و خط ۳۰۷ به بعد را پیدا کنید:

```javascript
const chapters = [
    {
        number: 1,
        title: "عنوان فصل ۱",
        file: "chapter01.html",
        available: false  // 👈 به true تغییر دهید
    },
    // ...
];
```

برای هر فصلی که فایلش را آماده کردید، `available` را به `true` تغییر دهید.

---

## گام پنجم: اجرا و تست

### تست محلی (Local):

**گزینه ۱: Python HTTP Server (پیشنهادی)**
```bash
# در پوشه پروژه این دستور را اجرا کنید:
python -m http.server 8000

# سپس مرورگر را باز کنید و بروید به:
# http://localhost:8000
```

**گزینه ۲: Live Server (VS Code)**
- اگر VS Code دارید، افزونه "Live Server" را نصب کنید
- روی `index.html` کلیک راست کنید
- "Open with Live Server" را انتخاب کنید

---

## گام ششم: آپلود در GitHub Pages

### 1. ساخت Repository

```bash
# در پوشه پروژه:
git init
git add .
git commit -m "Initial commit"
```

### 2. ایجاد Repository در GitHub

- به GitHub بروید
- New Repository بزنید
- نام دلخواه بگذارید (مثلاً `hematology-notes`)
- Public انتخاب کنید
- Create Repository کلیک کنید

### 3. آپلود کد

```bash
git remote add origin https://github.com/USERNAME/REPO-NAME.git
git branch -M main
git push -u origin main
```

### 4. فعال‌سازی GitHub Pages

- Settings → Pages
- Source: `main` branch
- Folder: `/ (root)`
- Save

✅ سایت شما آماده است در:
```
https://USERNAME.github.io/REPO-NAME/
```

---

## ❓ سوالات متداول

### فصل بارگذاری نمی‌شود؟
✅ بررسی کنید:
- پوشه `chapters` در کنار `index.html` هست؟
- نام فایل صحیح است؟ (مثلاً `chapter10.html` نه `Chapter10.html`)
- در `index.html` مقدار `available: true` قرار داده‌اید؟

### صفحه خالی نمایش می‌دهد؟
✅ از Python HTTP Server یا GitHub Pages استفاده کنید، نه مستقیماً فایل را باز کنید.

### چطور عنوان فصول را تغییر دهم؟
✅ در `index.html` در آرایه `chapters`، مقدار `title` را تغییر دهید.

---

## 📞 کمک بیشتر

- راهنمای کامل: `README.md`
- نمونه فایل فصل: `chapter01_template.html`
- فصل آماده: `chapter10.html`

---

**موفق باشید! 🎉**
